You are given one Ontario Highway Traffic Act law section as JSON.

Return exactly one JSON object. Do not wrap it in Markdown.

The JSON object must contain:
- "section_number": the input section number
- "title": the input title
- "summary": a concise plain-language summary
- "requirements": an array of concrete legal requirements or prohibitions

Input law:
{{law_json}}

